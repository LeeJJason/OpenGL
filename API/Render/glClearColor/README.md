## C Specification

```c
void glClearColor(	GLfloat red,
 	GLfloat green,
 	GLfloat blue,
 	GLfloat alpha);
```

## Parameters

**`red`**,**`green`**,**`blue`**,**`alpha`**

指定清除颜色缓冲器时使用的红色、绿色、蓝色和透明度。初始值为 0。

